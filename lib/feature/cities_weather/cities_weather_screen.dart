import 'package:flutter/material.dart';

class CitiesWetherScreen extends StatelessWidget {
  const CitiesWetherScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.red,
    );
  }
}
